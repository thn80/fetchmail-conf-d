#!/bin/sh

set -e

if [ "$1" = configure ]; then
	mkdir -p /etc/fetchmail-imap-idle.d/
	chmod 600  /etc/fetchmail-imap-idle.d/

	if [ -d /sys/fs/cgroup/systemd ] ; then
		systemctl --system daemon-reload >/dev/null || true
	fi
fi


#DEBHELPER#

exit 0
